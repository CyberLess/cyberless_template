// =========================================================
// Gulp Task: Deploy
// =========================================================

module.exports = function(gulp, plugins, path) {
    return () => {
	    var stream = 
			// -------------------------------------------- Start Task
			    gulp.src('')
			// ---------------------------------------------- End Task
	    return stream;
    };
};